<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
</head>
<body>
  <%- include("includes/navbar") %>
  <ul>
    <% messages.forEach(message => { %>
      <li><strong><%= message.user %>:</strong> <%= message.text %> <em>(<%= message.added.toLocaleString() %>)</em></li>
    <% }); %>
  </ul>
</body>
</html>
